Regression audit: no-vpnchain branch vs main
Date: 2026-06-02

=== Files changed (main -> no-vpnchain) ===
D .github/workflows/ci.yml
D module/scripts/ovpn.service
M module/scripts/ssh.iptables
D module/scripts/vpnchain.iptables
D module/vpnchain/configs/.gitkeep

=== ssh.iptables diff summary ===
- Removed cleanup_vpnchain() function (6 lines)
  * Was removing vpnchain uid-range mangle marks, NAT RETURN, MASQUERADE, ip rule/route table 200
- Removed cleanup_vpnchain call from cleanup_all()
- Removed vpnchain.active check at top of enable|renew block
  * Previously: if vpnchain.active marker exists, skip SSH device-redirect and exit 0 (socks-only mode)
  * Now: always proceeds with full SSH device routing
- Removed vpnchain.active check in disable block
  * Previously: skipped captive-portal restoration when vpnchain.active present
  * Now: always restores captive-portal settings on disable

=== Verification: no vpnchain code remains in ssh.iptables ===
PASS - Zero references to vpnchain, vpnchain.active, cleanup_vpnchain, uid 10000-99999,
mark 0x1, table 200, MASQUERADE, or tun0 in the no-vpnchain version.

=== SSH routing modes still present ===
PASS - All three modes intact:
  1. REDIRECT (nat, TCP only) - start_redirect / stop_redirect
  2. TPROXY (mangle, TCP+UDP) - start_tproxy / stop_tproxy
  3. TUN/TUN+UDPGW (mangle + tun device) - start_tun_bypass / stop_tun_bypass

All supporting infrastructure present:
  - Capability probing (TPROXY, conntrack, connmark, ipset, ip6 nat)
  - Capability downgrades (tproxy -> redirect fallback)
  - DNS hijack (SSHC_DNS_HIJACK / SSHC_DNS_HIJACK6)
  - DNS-through-tunnel (SSHC_DNS, UDP:53 -> 127.0.0.1:5353, uid 0 bypass)
  - QUIC block (UDP 443/80 DROP)
  - Hotspot sharing (SSHC_HOTSPOT / SSHC_HOTSPOT6)
  - Intranet bypass (RFC1918 + link-local + loopback)
  - IPv6 enable/disable with policy routing
  - Runtime snapshot save/load
  - Captive portal disable on enable, restore on disable

=== Remaining vpnchain references in other files ===
These files still contain vpnchain references (NOT in ssh.iptables, but elsewhere):
  module/uninstall.sh  - line 12, 36
  module/service.sh   - line 37
  module/customize.sh - lines 41-42, 81, 97-110
  build.sh            - line 54

These are build/install/uninstall scripts that reference deleted vpnchain
files and module IDs. They are likely stale references that should be
cleaned up, but they do not affect runtime SSH routing behavior.

=== Regression verdict ===
PASS - ssh.iptables routing logic is intact and fully functional.
No vpnchain code remains in ssh.iptables.
The enable path no longer skips SSH rules when vpnchain.active is set
(because that marker no longer exists), so SSH device routing is always
applied. The disable path always restores captive-portal settings.
This is the expected behavior for the no-vpnchain branch.
